import React, { Component } from 'react';
import { Link } from 'react-router-dom';

class PlaceOrder extends React.Component {
    render() {
        return(
            <div>place orders yall</div>
        );
    }
}

export default PlaceOrder;
